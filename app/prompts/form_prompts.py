# -*- coding: utf-8 -*-
"""
=============================================================================
Prompt Templates - 灵档智能填表
=============================================================================
文件化 Prompt，支持热替换，无需重启服务。

用法:
    from app.prompts.form_prompts import FORM_EXTRACT_PROMPT, FORM_GENERATE_PROMPT
    prompt = FORM_EXTRACT_PROMPT.format(document_text=text)

约定:
    - 所有 Prompt 用 triple-quote 字符串定义
    - 预留 {{变量}} 供运行时 format
    - 修改后保存立即生效（模块重载或重启）
=============================================================================
"""

# =============================================================================
# 1. 文档信息提取 Prompt（替代 Dify form-extract）
# =============================================================================

FORM_EXTRACT_PROMPT = """你是一个专业的文档信息提取助手。请从以下参考文档中提取所有可用于填写表格的关键信息，并以严格的 JSON 格式返回。

## 提取规则
1. **只提取文档中明确出现的信息**，绝不推测、不编造
2. 信息不存在时，字段值填 null，不要省略字段
3. 日期统一格式：YYYY-MM-DD
4. 金额只填数字（不带单位、不带逗号）
5. 人名保留完整（含姓氏）
6. 如果文档中有多个同类信息（如多个获奖），用列表形式返回

## 输出格式要求
- 必须返回合法的 JSON 对象
- 不要添加 markdown 代码块标记（如 ```json）
- 不要添加任何解释性文字
- 不要添加"以下是提取结果"等前缀
- 直接输出 JSON 字符串

## 示例输出
{{"姓名": "张三", "学号": "20240001", "专业": "计算机科学与技术", "成绩": 95, "获奖名称": ["数学建模一等奖", "ACM银奖"], "申请理由": "本人在学习上勤奋刻苦..."}}

---

【参考文档内容】
{document_text}

请提取所有关键信息并以 JSON 格式返回：
"""


# =============================================================================
# 2. 表格字段匹配与填写 Prompt（替代 Dify form-generate）
# =============================================================================

FORM_GENERATE_PROMPT = """你是一个表格智能填写助手。请根据已提取的信息和空白表格的结构，生成精确的填写指令。

## 任务说明
1. 分析空白表格的字段名（如"姓名"、"学号"、"申请理由"）
2. 将已提取的信息匹配到对应字段
3. 如果某个表格字段在提取信息中找不到对应值，标记为"[待补充]"
4. 返回每个字段的最终填写值

## 输出格式要求
- 必须返回合法的 JSON 对象
- 结构: {{"字段名": "填写值", ...}}
- 不要添加 markdown 代码块标记
- 不要添加任何解释性文字
- 直接输出 JSON 字符串

## 示例
输入:
- 提取信息: {{"姓名": "张三", "学号": "20240001"}}
- 表格字段: ["姓名", "学号", "班级"]

输出:
{{"姓名": "张三", "学号": "20240001", "班级": "[待补充]"}}

---

【已提取的信息】
{extracted_json}

【空白表格的字段列表】
{field_list}

请生成填写结果并以 JSON 格式返回：
"""


# =============================================================================
# 3. 自动规整 Prompt（替代 Dify organize）
# =============================================================================

ORGANIZE_PROMPT = """你是一个文档自动规整助手。请分析以下文件信息，给出分类、命名和标签建议。

## 分析维度
1. **分类建议**：根据内容判断应归入哪个目录（如"工作/合同/2024"）
2. **重命名建议**：给出规范的文件名（含扩展名）
3. **标签建议**：提取 3-5 个关键词作为标签
4. **摘要**：50 字以内的内容摘要
5. **置信度**：0-1 之间的整体置信度

## 输出格式要求
- 必须返回合法的 JSON 对象
- 结构:
{{
  "category": {{"suggestedSubPath": "建议路径", "reason": "理由", "confidence": 0.9}},
  "rename": {{"suggestedName": "建议文件名", "reason": "理由", "confidence": 0.9}},
  "tags": [{{"tagName": "标签名", "reason": "理由", "confidence": 0.8}}],
  "summary": "摘要",
  "keywords": ["关键词1", "关键词2"],
  "confidence": 0.9
}}
- 不要添加 markdown 代码块标记
- 不要添加任何解释性文字
- 直接输出 JSON 字符串

---

【文件信息】
文件名: {file_name}
文件内容预览（前2000字）:
{file_content_preview}

请给出规整建议并以 JSON 格式返回：
"""


# =============================================================================
# 4. 文本提取（非 OCR，用 Apache Tika 或 python-docx）
# =============================================================================
# 说明：此 Prompt 用于从 Word/Excel/PDF 中提取纯文本，供上述 Prompt 使用
# 实际实现用 python-docx / openpyxl / PyPDF2，不走 LLM

TEXT_EXTRACTION_NOTE = """
文本提取说明：
- Word (.docx): 用 python-docx 提取段落和表格文本
- Excel (.xlsx): 用 openpyxl 提取所有单元格文本
- PDF: 用 PyPDF2 / pdfplumber 提取文本层
- 扫描件: 用 PaddleOCR（本轮搁置）
"""
